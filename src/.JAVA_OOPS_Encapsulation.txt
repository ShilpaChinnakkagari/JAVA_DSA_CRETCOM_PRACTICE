I. ENCAPSULATION :
===================
Definition : process of Wrapping up of data(variables) and methods(functions) into a single entity/unit
called: Encapsulation Class.
--> By default, Java supports encapsulation -- default (no key word)
    (Java supports encapsulation through classes, access modifiers,
    and getter/setter methods.)

Analogy :
        ----------------------------------------------
        |       Data member      | Methods           |
        |                        |                   |
        ----------------------------------------------

--> It is described as a protective barrier that prevents data from being directly accessed by code outside the class.

------------------------------------------------------------------------------------------------------------------------
II. ACCESS MODIFIERS IN JAVA
=============================
Java provides four access modifiers: private, default, protected, and public

==> In Java, access control is used to restrict access to class members (fields, methods, constructors, classes)
    based on their access modifiers. This ensures encapsulation, a key principle of object-oriented
    programming, where the internal state of an object is protected from unauthorized access.

There are 4 access modifiers:
    ----------------------------------------------------------------------------------------------
    | Access Modifier | Same Class | Same Package | Subclass (Different Package) | Other Package |
    | --------------- | ---------- | ------------ | ---------------------------- | ------------- |
    | **public**      | ✅         | ✅           | ✅                          | ✅            |
    | **protected**   | ✅         | ✅           | ✅                          | ❌            |
    | **default**     | ✅         | ✅           | ❌                          | ❌            |
    | **private**     | ✅         | ❌           | ❌                          | ❌            |
    ----------------------------------------------------------------------------------------------

    Which access modifier is most restrictive?
    ✅ private

    Which access modifier is least restrictive?
    ✅ public

    1. public: Accessible from anywhere (other classes, packages, etc.).
            public int age;
    2. private: Accessible only within the same class.
            private String name;
    3. protected: Accessible within the same package and by subclasses (even if they are in
    different packages).
            protected double salary;
    4. default (no modifier) : Accessible only within the same package.
            int count

    ** How to get private fields in another class
    ===============================================
    Using Getters and Setters methods

    • Why Use Private Members with Getters and Setters?
    ➢ Encapsulation: Protects the internal state of an object.
    ➢ Validation: Allows you to add validation logic in setter methods.
    ➢ Read-Only or Write-Only Access: You can provide only a getter (read-only) or only
    a setter (write-only).
    ➢ Flexibility :  You can change the internal implementation without affecting other
                    classes.

------------------------------------------------------------------------------------------------------------------------

III. How can Java achieve Encapsulation :
=========================================
Encapsulation is achieved in Java by hiding data using the private access modifier and providing controlled access
through public getter and setter methods

1. Declare variables as private.
2. Provide public getter and setter methods.
3. Access data only through getters and setters.

        Encapsulation
              ↓
        Data Hiding
              ↓
        private variables
              +
        Getter/Setter methods
Example :
---------
    ATM Machine

    What you see:
    -------------
    Withdraw()
    Deposit()

    What is hidden:
    ---------------
    Account Balance
    PIN Verification
    Bank Database

------------------------------------------------------------------------------------------------------------------------

IV. Advantages of Encapsulation :
==================================
--> we can restrict the accessibility of data members in different classes except by the setter and getter method.
--> The main Advantages of encapsulation is code readability.
--> We can perform some validation with the usage of setter method.
        1. Data Hiding
        2. Better Security
        3. Controlled Access through getters/setters
        4. Easy Validation
        5. Better Maintainability

------------------------------------------------------------------------------------------------------------------------

V. DIFFERENCE BETWEEN ENCAPSULATION AND ABSTRACTION :
=====================================================
---------------------------------------------------------------------------------------------------------
| Encapsulation                                        | Abstraction                                    |
| ---------------------------------------------------- | ---------------------------------------------- |
| Hides data                                           | Hides implementation                           |
| Achieved using private variables and getters/setters | Achieved using abstract classes and interfaces |
| Focuses on security                                  | Focuses on simplicity                          |
---------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------